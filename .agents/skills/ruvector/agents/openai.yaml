interface:
  display_name: "Ruvector"
  short_description: "Repo-specific patterns and workflows for ruvector"
  default_prompt: "Use the ruvector repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true